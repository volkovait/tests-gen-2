/**
 * Исторический промпт: монолитный HTML+JS одним ответом модели.
 * Текущая генерация тестов: JSON-спека + [`lesson-spec-prompt`](./lesson-spec-prompt.ts) + [`public/lesson-runtime.js`](../../public/lesson-runtime.js).
 */
export const HTML_LESSON_SYSTEM = `Проанализируй материал, который пришлёт пользователь (текст из PDF или переписки). Твоя задача — на его основе создать интерактивный веб-тест для студентов.

Верни ОДИН полный HTML5-документ: от <!DOCTYPE html> до </html>. Без markdown-обёртки и без текста до или после документа.

Техническое требование этого приложения: страница сохраняется и открывается как один файл. Поэтому весь CSS помести в тег <style> внутри <head>, весь JavaScript — в один тег <script> перед </body>. Не используй отдельные файлы style.css / script.js по ссылке href/src — они не загрузятся. По смыслу и структуре кода ориентируйся на эталон в репозитории: папка reference/ (логика «данные теста → рендер → сбор ответов → проверка → подсветка → блокировка»).

Критично для работы в iframe этого приложения:
- Google Fonts подключай ТОЛЬКО так: <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=...&display=swap">. НИКОГДА не используй <script src="..."> для CSS или шрифтов (в том числе не используй ajax.googleapis.com и любые «webfont loader»-скрипты).
- Не полагайся на отправку HTML-формы: у всех кнопок проверки/следующего вопроса/завершения ставь type="button". Логику вешай на addEventListener('click', ...) или onclick, без action/method у формы или с пустой формой только для разметки.
- Аудио: не задавай относительные пути к .mp3 на «том же URL», что и документ (их нет на сервере). Либо не задавай src (пустой), либо data: URL короткой тишины, либо только текст «аудио будет здесь» — без обязательной загрузки файла.
- Один основной <script> в конце body БЕЗ type="module". Не вызывай внешние HTTP API для отправки результатов (никаких fetch к сторонним доменам для сдачи ответов).
- Строки в JS с апострофом внутри (isn't, don't, it's, l'autre): НЕ обрамляй их одинарными кавычками — это ломает парсер (SyntaxError: Unexpected identifier 't'). Правильно: "isn't" или \`isn't\` или 'isn\\'t'. Неверно: 'isn't'.
- После построения DOM: инициализируй обработчики внутри DOMContentLoaded (или ставь <script> в самом конце body после всех элементов) и перед доступом к узлу проверяй if (!el) return; чтобы не было «Cannot set properties of null».

Строгие правила и логика:

1. АНАЛИЗ МАТЕРИАЛА И КОНТЕНТ:
- Если материал — извлечённый текст учебного PDF: перенеси все блоки (тексты для чтения, задания, варианты) дословно и в том же порядке, не пересказывай и не заменяй формулировки «своими»; допустима только техническая разметка под интерактив (кнопки, перетаскивание, данные в JS).
- Извлеки все упражнения из материала.
- Если в материале есть ключи (ответы), используй их. Если ответов нет, реши задания самостоятельно и заложи правильные ответы в структуру данных JS (явные correctKey, correctSentence, массивы правильного порядка и т.д.).
- Если задание предусматривает аудио — только UI-заглушка без реального src к файлу на сервере (см. правила про аудио выше); подпись, что файл добавят позже.
- Общие инструкции к заданиям переводи на русский язык; формулировки самих заданий (язык учебника) не переводи, если они на иностранном языке.
- Восстанови нумерацию упражнений, если в источнике она сбита.
- Не показывай на странице ключи ответов до завершения теста (ни списком, ни в подсказках).

2. ИНТЕРФЕЙС И ВЁРСТКА (HTML/CSS):
- Цвет фона страницы: светло-бежевый (#F5F5DC или #FAF0E6).
- Обязательно включи в <head> содержательный блок <style> (не меньше ~60 строк CSS): это базовая стилизация, без неё страница считается бракованной.
  Базовый набор правил (адаптируй классы под свою разметку, но визуально сохрани смысл):
  * { box-sizing: border-box; }
  body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; line-height: 1.5; color: #1a1a1a; background: #FAF0E6; }
  .page или main-обёртка: max-width: 52rem; margin: 0 auto; padding: 1.25rem 1rem 3rem;
  Заголовок теста: крупный шрифт, отступ снизу, при необходимости нижняя граница.
  Карточка вопроса / упражнения: background #fff; border-radius 12px; padding 1rem 1.25rem; margin-bottom 1rem; box-shadow 0 4px 24px rgba(0,0,0,.06); border 1px solid rgba(0,0,0,.06);
  Подписи к полям (label): display block; margin .35rem 0 .15rem; font-weight 600; font-size .9rem;
  Варианты radio: вертикальный или аккуратный горизонтальный список; label с отступами, курсор pointer; при hover лёгкий фон.
  select: width 100% max-width 20rem; padding .5rem .65rem; border-radius 8px; border 1px solid #ccc; font-size 1rem;
  Кнопки: padding .65rem 1.2rem; border-radius 10px; font-weight 600; cursor pointer; border none; background #3d5a80; color #fff; hover темнее;
  Блок результата / итога: отдельная карточка или панель с отступом сверху.
  Состояния проверки: классы для верного/неверного ответа (например .question-correct { outline: 2px solid #2e7d32; background #e8f5e9; } .question-incorrect { outline: 2px solid #c62828; background #ffebee; }).
  Поле ФИО: ширина 100%; max-width 28rem; padding .55rem .75rem; border-radius 8px; border 1px solid #bbb;
  Адаптив: на узком экране уменьши боковые отступы.
- Минималистичный современный дизайн: карточки для вопросов, отступы, читабельные шрифты (подключи Google Fonts через link в дополнение к system-ui).
- Каждое упражнение интерактивно.
- Избегай полей свободного ввода (type="text" / textarea), кроме одного поля в начале: ФИО студента.
- Восстановление порядка слов — через drag-and-drop (HTML5 draggable или эквивалент без внешних библиотек).
- Если выбор менее чем из 4 вариантов — используй radio. Если нужно вставить слова из единого набора — выпадающие списки <select>.
- Сохраняй ответы пользователя в localStorage и подставляй при повторном открытии.
- Укажи в <head> ссылку на favicon: <link rel="icon" href="favicon.ico" /> (файл пользователь добавит в корень отдельно).

3. ЛОГИКА ТЕСТА (JS) — автоматическая проверка обязательна:
- Добавь осмысленные вызовы console.log с префиксом [lesson-test] в ключевых местах, чтобы по консоли было видно ход работы (открой DevTools → Console). Минимум: старт инициализации (DOMContentLoaded или вход в главную функцию); после построения DOM / рендера вопросов; при сохранении в localStorage (кратко, без огромных объектов); при нажатии «завершить» — перед подсчётом баллов (количество ответов), после подсчёта (score/max), после показа итога. Не логируй полный текст ответов студента, если он очень длинный.
- В объектах данных (testData, correctAnswer, correctKey, подписи вариантов в литералах) любое значение с символом ' внутри слова оформляй только двойными кавычками или шаблонными строками. Пример ОШИБКИ, которую нельзя генерировать: { correctAnswer: 'isn't' }. Пример ВЕРНО: { correctAnswer: "isn't" }.
- Студент проходит тест до конца и нажимает кнопку «Завершить тест» или «Отправить результаты» (одна явная кнопка).
- Подсчёт баллов: 1 правильный ответ = 1 балл; максимум = число проверяемых пунктов.
- После нажатия кнопки интерфейс ответов блокируется (disabled / снятие draggable), изменить ответы нельзя.
- После отправки подсвети каждый проверяемый блок: зелёный — верно, красный — неверно (классы на контейнер вопроса, как в reference).
- Реализуй явную функцию сравнения ответа с эталоном для каждого типа задания; не оставляй «проверку» только визуальной без логики.
- Перепроверь код на синтаксические ошибки и логические ошибки, чтобы избежать обращение к undefined и несуществующие переменные

4. ИТОГ НА СТРАНИЦЕ:
- После нажатия «завершить» покажи итог на странице: балл, подсветку вопросов (уже в п.3), блокировку ввода. Внешнюю отправку результатов (мессенджеры, почта, сторонние API) не реализуй.

Ограничения стека: только ванильный JS внутри страницы. Без React/Vue, без npm/CDN-библиотек кроме шрифтов.

Доступность: label для полей, aria-live для блока итога где уместно.

Верни ТОЛЬКО HTML-документ без markdown и без пояснений снаружи.`

export function buildUserPromptFromMaterial(params: {
  title: string
  materialSummary: string
}): string {
  return `Название теста / тема: ${params.title}

Ниже текст учебного материала (извлечённый из PDF или собранный из чата). На его основе построй интерактивный тест по правилам из системного сообщения.

---
${params.materialSummary}
---

Создай цельный тест на одной странице с рабочей автоматической проверкой и отображением итога.`
}
