import { LESSON_SPEC_VERSION } from '@/lib/lesson-spec/schema'

/** Системный промпт: только JSON-спецификация теста, без HTML/JS. */
export const LESSON_SPEC_SYSTEM = `Ты составляешь структуру интерактивного языкового теста для веб-приложения.

Верни ОДИН JSON-объект без markdown-ограждения и без текста до или после JSON.

Схема (поле version строго ${LESSON_SPEC_VERSION}):
{
  "version": ${LESSON_SPEC_VERSION},
  "title": "краткое название теста",
  "subtitle": "опционально, подзаголовок",
  "googleFontsHref": "опционально, только ссылка вида https://fonts.googleapis.com/css2?family=...&display=swap",
  "parts": [
    {
      "title": "название части",
      "exercises": [
        {
          "title": "название упражнения",
          "instruction": "опционально, инструкция на русском",
          "audio": false,
          "inputKind": "radio" | "select" | "checkbox" | "wordOrder" | "gapDrag" | "matchPairs",
          "readingPassage": {
            "title": "опционально",
            "instruction": "опционально",
            "paragraphs": ["абзац текста для чтения", "..."]
          },
          "questions": [...]
        }
      ]
    }
  ]
}

Правила контента:

Если в пользовательском материале есть раздел **«## План типов заданий (инструменты пайплайна — выполняй буквально)»**:
- Следуй JSON намерения и таблице **partIndex → inputKind**; для каждой части нарезки документа выходной JSON должен иметь согласованный тип упражнений (**одинаковый inputKind** для всех exercises в соответствующем элементе массива parts той же позиции, что и часть материала).
- Поле **trueFalseOnly** в плане означает: только варианты True/False (или Верно/Неверно) на L2, без MCQ с несколькими смысловыми фразами.

Антигаллюцинации — дословность текста чтения и язык L2 (критично, нарушение недопустимо):
- **L2 определяется по основному учебному отрывку** (абзацы на иностранном языке, диалог, упражнения из учебника), а **не** по языку короткой мета-просьбы пользователя на русском (например «создай true/false»). Если отрывок для чтения на **английском** — все проверяемые **prompt**, **gapTemplate**, **wordBank**, **correctSentence**, **matchLeftItems**, **gapCorrectToken(s)** при необходимости, а также **options[].text** и **matchRightOptions[].text** (включая подписи True/False) — **на английском**; **запрещено** писать сами утверждения вопросов или варианты ответов **на русском**. Аналогично для другого L2: стимулы только на L2, не переводить их на русский «для ясности».
- **readingPassage.paragraphs:** копируй абзацы из учебного текста источника **пословно** (те же слова и порядок предложений). Допустимо только: убрать лишние пробелы/переносы из OCR, склеить разорванные строки **без изменения слов**. **Запрещено:** пересказ, перевод на другой язык, замена синонимами, «улучшение» стиля, сокращение предложений, подмена примеров выдуманными репликами.
- Вопросы проверяют **только факты**, которые следуют из дословного текста в readingPassage; не вводи новых персонажей, цифр и фактов, которых нет в источнике.

Приоритет явных инструкций пользователя (если в материале сверху или в начале есть просьба: формат, число вопросов, тип заданий — true/false, «сделай N вопросов» и т.п.):
- Эти требования важнее общего режима «только перенос из PDF»: выполни их буквально (число, формат), не сокращая число вопросов ради краткости.
- Текст для чтения из источника при этом переноси в readingPassage.paragraphs полностью, не деля на части, дословно по словам источника (L2), без сокращения абзацев ради объёма. Задания к тексту рапологай после текста.

Неучебный шум в извлечённом PDF (обложка, оглавление, колонтитулы, ISBN, реклама, юридические вставки издательства, пустые повторы):
- Не включай такие фрагменты в тест и не придумывай по ним вопросы; сосредоточься на учебных блоках: тексты для чтения, лексика по теме, нумерованные упражнения из учебника.

Верность источнику (обязательно, если материал — текст из загруженного PDF / учебника, а не короткая тема в чате):
- Перенеси на интерактивную страницу все учебные блоки из материала: отрывки для чтения, примеры, нумерованные упражнения, варианты ответов — в том же смысле и по возможности дословно, в логическом порядке как в PDF. Это не пересказ и не «улучшение» учебника: не подменяй формулировки своими, не сокращай параграфы ради краткости, не объединяй и не дроби упражнения без оснований в источнике.
- Допустима только техническая нормализация под JSON и вёрстку: убрать HTML; варианты вместо подчёркивания оформить как "a / b"; разбить длинный общий текст на readingPassage + вопросы, не меняя слова внутри абзацев и вопросов.
- Если какой-то фрагмент PDF нельзя точно вписать в схему (нет подходящего inputKind), приблизься к смыслу средствами схемы, но сохрани оригинальные слова в prompt / options / gapTemplate насколько возможно; не заменяй задание другим по теме.
- Если в материале нет готовых упражнений (например пользователь только описал тему в чате, или есть только отрывок для чтения и мета-просьба составить тест) — составь задания сам с явными правильными ответами. При свободной генерации по тексту чтения без указания числа сделай **не менее 8** проверяемых вопросов суммарно (разумно распределив по частям/упражнениям); в каждом упражнении с generated-вопросами — **не менее 5** вопросов, если объём текста это позволяет.

- Если в пользовательском сообщении есть блок «Правильные ответы от пользователя», сопоставь его с заданиями и используй как эталон для correctKey / correctKeys / correctSentence / gapCorrectToken(s) / matchCorrectKeys там, где это удаётся.
- Если такого блока нет или он пуст, а в исходном материале нет явных правильных ответов, самостоятельно выбери корректные ответы по смыслу материала и своим языковым знаниям.
- Не включай в JSON ключи ответов для студента отдельным списком; только поля correctKey / correctKeys / correctSentence / gapCorrectToken / gapCorrectTokens / matchCorrectKeys внутри вопросов.

Язык (обязательно для преподавателей иностранного языка):
- Определи язык учебного материала (L2) по **непрерывному тексту отрывка для чтения и учебным примерам**, а не по языку заголовка карточки или мета-просьбы на русском. Не подменяй L2 на русский там, где в материале уже дан иностранный текст.
- На русском пиши только то, что объясняет студенту, что делать: поля instruction у упражнения, readingPassage.instruction, readingPassage.title (если заполняешь), заголовки частей parts[].title и при необходимости exercises[].title (краткие рабочие названия блоков можно оставить на русском).
- На языке материала (L2), как в источнике, без перевода на русский: readingPassage.paragraphs (текст для чтения), prompt у вопроса, options[].text, gapTemplate, wordBank, correctSentence, matchLeftItems и matchRightOptions[].text — дословно или минимально адаптируя пунктуацию, но не переводя смысл на русский.
- Если в материале есть общий отрывок для чтения и задания «по тексту»: вынеси отрывок один раз в readingPassage этого упражнения; вопросы привяжи к нему. В instruction можно по-русски написать «Ответьте по тексту выше» / «Используйте абзац выше»; в prompt формулируй сам вопрос на L2 (как в учебнике). Не повторяй весь отрывок в каждом prompt, если достаточно отсылки к тексту выше.
- Исключение: если исходник уже смешивает русский и L2 (методичка), сохраняй ту же смесь, не унифицируй насильно.
- audio: true только если нужна заглушка «есть аудирование»; реального файла нет — это только флаг UI.
- readingPassage: опционально, перед этим упражнением покажется текст; все строки в paragraphs — обычный текст (без HTML).

Критично — только обычный текст, без HTML и без дублирования буквы варианта в text:
- В полях title, subtitle, prompt, instruction, option.text, paragraphs НЕ используй HTML-теги (<u>, <b>, <span>, …). Подчёркивание вариантов в предложении делай косой чертой или скобками: "has / have", "are / is", а не <u>…</u>.
- Для radio/select у каждого варианта key — ОДНА заглавная латинская буква по порядку: "A", "B", "C", "D". Поле text — только формулировка варианта БЕЗ префикса "A)" / "a)" / дублирования буквы (не пиши text: "A) goes", пиши text: "goes"; не пиши key "are" с text "are" — используй key "A" и text "are").

Типы вопросов (на уровне упражнения inputKind одинаков для всех вопросов в exercises):
1) "radio" или "select": у каждого вопроса поля id, prompt, options: [ { "key": "A", "text": "..." }, ... минимум 2 ], correctKey — один из key.
   **Если пользователь просил true/false (или верно/неверно):** у каждого вопроса в options **только два** варианта — подписи «True» и «False» на L2 (или «Верно» / «Неверно», если L2 так задан в материале). **Запрещено** подменять этот формат обычным MCQ с тремя–четырьмя разными текстовыми ответами-фразами: проверяемое утверждение целиком — только в **prompt**, а не в options.
2) "checkbox": несколько верных вариантов; у каждого вопроса options (минимум 2) и correctKeys: массив из одной или нескольких букв key (например ["A","C"]).
3) "wordOrder": порядок слов / сборка диалога по фрагментам (формулировки вроде "Put the words in the correct order", нумерованные реплики со словами, разделёнными символом "/"). Поля у каждого вопроса: id, prompt (цитата задания с L2), wordBank (минимум 2), correctSentence — эталон на L2 теми же лексическими единицами, что в банке.
4) "gapDrag": вставить слово(а) из банка в пропуск(и) методом перетаскивания. Поля у каждого вопроса: id, prompt (кратко), gapTemplate — строка с маркерами "___" (один или несколько в одном предложении/диалоге: «Complete with words from the box»), wordBank (минимум 2; допускай лишние слова-дистракторы). При **одном** пропуске задай gapCorrectToken (или gapCorrectTokens из одного элемента) — слово из wordBank; при **нескольких** пропусках подряд — gapCorrectTokens: массив по порядку слева направо для каждого "___".
5) "matchPairs": соединить части («Match the sentence halves», колонки 1–6 и A–F). Одно упражнение — один объект-вопрос на всё задание: id, prompt (инструкция с источника на L2), matchLeftItems: начала строк в заданном порядке — массив строк на L2, matchRightOptions: [ { key: "A", text: "…" }, … ] ключи заглавными латинскими по порядку, matchCorrectKeys: массив ключей правой колонки **в том же порядке**, что matchLeftItems (какое окончание к какому началу). Справа варианты в UI будут перемешаны; тексты сохраняй дословно из PDF.

Ограничения:
- Уникальные id вопросов во всём документе (латиница, цифры, подчёркивание).
- Не более 200 вопросов суммарно.
- Строки с апострофами внутри слова (it's, isn't) — допустимы в JSON в двойных кавычках.

Верни только JSON.`

export const MIN_FREE_GENERATION_QUESTIONS = 8

/** Эвристика: в материале явно запрошен формат верно/неверно — усиливаем user-промпт. */
export function materialExplicitlyRequestsTrueFalse(materialSummary: string): boolean {
  const text = materialSummary.toLowerCase()
  if (/\btrue\s*[/\s,–—-]+\s*false\b/.test(text)) return true
  if (/\bt\s*[/\s,–—-]+\s*f\b/.test(text)) return true
  if (/true\s+or\s+false/.test(text)) return true
  if (/верно\s*[/\s,–—-]+\s*неверно/.test(text)) return true
  if (/true\s*\/\s*false|true-false/.test(text)) return true
  if (/\bда\s*[/\s,–—-]+\s*нет\b/.test(text) && /(тест|вопрос|задан|упражнен)/.test(text)) return true
  return false
}

export function buildUserPromptForLessonSpec(params: {
  title: string
  materialSummary: string
  /** Необязательный текст с правильными ответами в свободной форме. */
  correctAnswersHint?: string
}): string {
  const hint = params.correctAnswersHint?.trim()
  const hintSection =
    hint && hint.length > 0
      ? `

Правильные ответы от пользователя (свободная форма; используй как эталон, сопоставляя с вопросами там, где это возможно):
---
${hint}
---
`
      : ''

  const trueFalseReminder = materialExplicitlyRequestsTrueFalse(params.materialSummary)
    ? `

КРИТИЧНО — запрошен формат TRUE/FALSE (верно/неверно):
Собери **все** проверяемые вопросы в упражнениях с inputKind "radio"; у **каждого** вопроса ровно два варианта в options: на L2 тексты «True» и «False» (или пара «Верно» / «Неверно», если язык задания не английский). В prompt — одно утверждение по тексту чтения. **Не** делай обычный тест с тремя–четырьмя смысловыми вариантами ответа.
`
    : ''

  return `Название / тема теста: ${params.title}
${trueFalseReminder}
Ниже материал (из PDF или переписки). Построй JSON-спецификацию теста по правилам системного сообщения.

---
${params.materialSummary}
---${hintSection}

Верни один JSON-объект version ${LESSON_SPEC_VERSION} с заполненными parts, exercises и questions.

Если это извлечённый текст учебного PDF: сохрани все блоки с материалами и заданиями в неизменной формулировке (дословно по тексту источника), только разложи по полям схемы; не заменяй упражнения своими переосмыслениями.

Язык: инструкции и пояснения для студента — русский; формулировки заданий, тексты для чтения и варианты ответов — язык исходного материала (L2), без перевода на русский, если в источнике не сказано иное.

Если в материале выше явно указаны число и/или тип заданий (в т.ч. true/false, верно/неверно, диапазон вроде «8–10») — выполни эти требования буквально. Если явного числа нет, но нужно самому составить тест по тексту чтения без готовых пунктов из источника — сделай **не менее ${MIN_FREE_GENERATION_QUESTIONS}** проверяемых вопросов суммарно, с полным текстом чтения в readingPassage без сокращения.

Антигаллюцинации (повтор для пользовательского сообщения): учебный отрывок в readingPassage.paragraphs скопируй **дословно** из блока материала на L2; все **prompt** и варианты ответов — на **том же L2**, что отрывок (английский текст → английские утверждения), не переводя стимулы на русский.`
}

/** Дополнение уже валидной спеки, когда вопросов слишком мало (второй вызов модели). */
export function buildLessonSpecEnrichmentUserPrompt(params: {
  title: string
  materialSummary: string
  currentSpecWithoutRuntimeJson: string
  minTotalQuestions: number
}): string {
  const enrichmentTf = materialExplicitlyRequestsTrueFalse(params.materialSummary)
    ? `Если исходный материал — true/false: новые вопросы тоже только с двумя вариантами True/False на L2, не добавляй MCQ с несколькими фразами в options.
`
    : ''

  return `Режим доращивания спецификации теста (version ${LESSON_SPEC_VERSION}).

Название / тема: ${params.title}

Ниже исходный материал и уже сгенерированный JSON теста. В текущем JSON **слишком мало** проверяемых вопросов — нужно **не менее ${params.minTotalQuestions}** вопросов **суммарно по всему документу**.

${enrichmentTf}Требования:
- Верни ОДИН полный JSON той же схемы, без поля runtime.
- Сохрани все существующие вопросы и тексты readingPassage как есть (не удаляй и не сокращай абзацы чтения); добавь только **новые** вопросы с **новыми уникальными** id.
- Новые вопросы должны опираться на материал и согласоваться с inputKind каждого упражнения; не нарушай правила системного промпта (язык, True/False как radio с двумя вариантами, и т.д.).
- **Язык:** новые prompt и варианты — только на L2 учебного отрывка (английский текст → английские утверждения); не переводь стимулы на русский. **Чтение:** не переписывай paragraphs readingPassage.

Исходный материал:
---
${params.materialSummary.trim().slice(0, 56_000)}
---

Текущий JSON (дополни недостающими вопросами):
---
${params.currentSpecWithoutRuntimeJson.slice(0, 120_000)}
---`
}

const EDIT_PLAIN_TEXT_MAX = 120_000

export function buildUserPromptForLessonEdit(params: {
  title: string
  currentTestPlainText: string
  editInstruction: string
}): string {
  const body = params.currentTestPlainText.trim()
  const trimmed = body.length > EDIT_PLAIN_TEXT_MAX ? `${body.slice(0, EDIT_PLAIN_TEXT_MAX)}…` : body
  return `Режим правки существующего теста.

Название / тема (карточка в списке): ${params.title}

Ниже — текстовое представление УЖЕ СГЕНЕРИРОВАННОГО теста (из HTML, теги убраны). Восстанови по нему полную структуру заданий и примени инструкцию пользователя. Сохрани всё, что пользователь явно не просил менять.

ТЕКУЩИЙ ТЕСТ:
---
${trimmed}
---

ИНСТРУКЦИЯ ПОЛЬЗОВАТЕЛЯ (выполни точечно, остальное без изменений):
---
${params.editInstruction.trim()}
---

Верни один JSON version ${LESSON_SPEC_VERSION} по правилам системного сообщения — полная спецификация теста после правок.`
}

export const LESSON_SPEC_REPAIR_SYSTEM = `Ты получишь невалидный или почти готовый JSON спецификации теста и список ошибок валидации (Zod).
Исправь ТОЛЬКО структуру данных: version, title, parts, exercises, questions, поля options/correctKey/wordBank/correctSentence/gapTemplate/gapCorrectToken/gapCorrectTokens/matchLeftItems/matchRightOptions/matchCorrectKeys, readingPassage.
Убери HTML из всех строк; в options задай key как "A","B","C", а text — только текст варианта без префикса "A)".
Не переписывай учебный текст из PDF ради правок: сохраняй формулировки вопросов, вариантов и абзацев чтения дословно, меняй только то, что нужно для валидности JSON.
Если в материале запрошен true/false — сохрани ровно два варианта True/False (на L2), не превращай в MCQ с несколькими текстовыми фразами в options.
Сохрани политику языка: инструкции и подписи для студента (instruction, readingPassage.instruction и т.п.) — на русском; стимулы, варианты, абзацы чтения, prompt — на языке материала (L2), без перевода на русский. **Не переводи** prompt и options.text на русский даже при правке структуры.
Не добавляй поле runtime — его добавит сервер.
Верни один исправленный JSON без markdown и без пояснений.`

export function buildRepairUserPrompt(params: { invalidJson: string; zodMessage: string }): string {
  return `Ошибки валидации:
${params.zodMessage}

Исходный текст (исправь):
${params.invalidJson.slice(0, 120_000)}`
}
